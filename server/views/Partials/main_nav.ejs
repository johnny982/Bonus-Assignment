<body>
    <!-- Nav Bar -->
    <nav class="navbar navbar-expand-lg navbar-dark bg-dark">
      <div class="container-fluid">
        <a class="titletext" href="/">WatchHistory</a>
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
          <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarSupportedContent">
          <ul class="navbar-nav ms-auto">
            
            <li class="nav-item">
              <% if(title=="Home") { %>
                  <a class="nav-link active" href="/">
              <% } else { %>
                  <a class="nav-link" href="/">
              <% } %>
                  <i class="fa-sharp fa-solid fa-house"></i> Home</a>
            </li>

              <li class="nav-item">
                <% if(title=="Movies" || title=="Add Movie" || title=="Edit Movie") { %>
                    <a class="nav-link active" href="/bookslist">
                <% } else { %>
                    <a class="nav-link" href="/bookslist">
                <% } %>
                    <i class="fa-sharp fa-solid fa-film"></i> Movies</a>
              </li>

              <% if(typeof displayName=='string' && displayName) {%>
                <li class="navbar-text">
                    <i class="fa-sharp fa-solid fa-user"></i>
                    Welcome, <%=displayName%>
                </li>
                <li class="nav-item">
                    <a href="/logout" class="nav-link">
                        <i class="fa-sharp fa-solid fa-right-from-bracket"></i>
                        Logout
                    </a>
                </li>
              <% } else { %>
                <li class="nav-item">
                    <a href="/login" class="nav-link">
                        <i class="fa-sharp fa-solid fa-right-to-bracket"></i>
                        Login
                    </a>
                </li>
              <% } %>
          </ul>
        </div>
      </div>
    </nav>